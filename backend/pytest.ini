[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    integration: tests that hit a real LLM provider (skipped without API keys)
    optional: tests that can be skipped during deployment (run manually post-deploy)
