# =============================================================================
# Autonomous Scheduler — Backend Dependencies
# =============================================================================
# Pinned to specific versions for reproducible installs.
# Install:  pip install -r requirements.txt
# =============================================================================

# --- Web framework + ASGI server ---
fastapi==0.141.1
uvicorn[standard]==0.32.1

# --- Database (SQLModel = Pydantic + SQLAlchemy) ---
sqlmodel==0.0.22
psycopg2-binary==2.9.10
SQLAlchemy==2.0.36
pgvector==0.3.6

# --- Auth (Supabase JWT verification) ---
PyJWT[crypto]==2.13.0
httpx==0.28.1

# --- LLM + memory ---
openai==3.1.0
groq==0.13.0
google-generativeai==0.8.3
mem0ai==2.0.18

# --- Config / env ---
python-dotenv==1.2.2
pydantic==2.10.3
pydantic-settings==2.7.0

# --- Billing (Stripe) ---
stripe==11.4.1

# --- Forms / multipart for file uploads (optional, but FastAPI will warn) ---
python-multipart==0.0.32

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
hypothesis==6.123.2
